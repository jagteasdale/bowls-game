# macOS
.DS_Store

# editor cruft
*.swp
*~

# note: the PICO-8 web export (bowls.html, bowls.js, index.html) IS committed on purpose
# so GitHub Pages can serve it. re-export from PICO-8 and re-commit to update the web build.

# kept LOCAL-ONLY: these draw affectionate (and pointed) satire from real club members,
# so we don't publish them on the (public) repo. they still exist in your working tree.
docs/characters.md
docs/social-sim.md
